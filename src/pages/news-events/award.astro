---
import Hero from "@/common/components/Hero.astro";
import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import BaseLayout from "@/layouts/BaseLayout.astro";

import { getNews } from "@/libs/api/post";
import NewsCard2 from "@/modules/news-events/components/news/NewsCard2.astro";
const news = await getNews("award", -1);

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<BaseLayout title={t("news.awards.title")}>
	<Hero title={t("news.awards.hero.title")} subtitle={t("news.awards.hero.subTitle")} />

	<main class="mx-auto max-w-(--breakpoint-xl) p-5">
		<div class="grid grid-cols-3 gap-10">
			{
				news.map((post: any) => {
					return <NewsCard2 news={post} />;
				})
			}
		</div>
	</main>
</BaseLayout>
